# Seed Data

Run the backend seed script after migrations:

```bash
cd server
npm run seed
```

The script inserts two realistic demo users:

- `+919876543210`
- `+919812345678`

Both use the development password `Password123`.
